import { css } from "@emotion/core";
import { RED } from "../../constants/COLORS";

const style = css`
  width: 80%;
  margin: 2rem auto;
  text-align: center;
  max-width: 70rem;
  font-size: 2rem;
  color: ${RED};
`;

export default style;
